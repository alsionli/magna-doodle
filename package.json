{
  "name": "magnetic-drawing-board",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y serve -l 3000",
    "start": "npx -y serve -l 3000"
  },
  "dependencies": {
    "puppeteer": "^24.38.0"
  }
}
